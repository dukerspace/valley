# Copy to `.env` at the repo root and adjust values as needed.
# All apps and packages load this single root env file.

# --- PostgreSQL (Prisma) ---
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/starter?schema=public"

# --- API (Hono / Bun) ---
API_HOST="127.0.0.1"
API_PORT="3001"
API_URL="http://127.0.0.1:3001"

# --- Frontend (TanStack Start / Vite) ---
FRONTEND_HOST="127.0.0.1"
FRONTEND_PORT="3000"
FRONTEND_URL="http://127.0.0.1:3000"

# Browser-exposed API base URL (must be prefixed with VITE_)
VITE_API_URL="http://127.0.0.1:3001"

# --- CORS ---
# Comma-separated origins allowed by the API (typically the frontend URL)
CORS_ORIGIN="http://127.0.0.1:3000"

# --- Runtime ---
NODE_ENV="development"
